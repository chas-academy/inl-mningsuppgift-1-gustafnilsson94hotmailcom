#include <stdio.h>
#include <stdlib.h>

int main()
{
    // The Line below must *NOT* be changed or test will fail!
    srand(1);

   
    //  Your code goes here

    // Skapar variabler
    int diceRolls[100];
    float averageSum;
    int totalSum = 0;

    return 0;
}
